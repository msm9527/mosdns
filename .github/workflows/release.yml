name: Release mosdns (manual)

on:
  workflow_dispatch:
    inputs:
      ref:
        description: '用于发布的分支或提交，默认 main'
        required: false
        default: 'main'
      tag_name:
        description: '发布 tag，留空时使用 .version'
        required: false
        default: ''

env:
  RELEASE_NAME_SUFFIX: "来源: mosdns-v5 + yyysuo/ph-v5"
  RELEASE_SUBTITLE: "副标题：原版来源 mosdns-v5，基于 yyysuo/mosdns 的 ph-v5 进行完善修改。"

permissions:
  contents: write

jobs:

  build-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          check-latest: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
          check-latest: true

      - name: Resolve tag name
        run: |
          BASE='${{ inputs.tag_name }}'
          if [ -z "$BASE" ]; then
            BASE=$(tr -d '\r\n' < .version)
          fi
          if [ -z "$BASE" ]; then
            echo "tag_name and .version are both empty" >&2
            exit 1
          fi
          echo "VERSION=$BASE" >> $GITHUB_ENV

      - name: Read Go version from go.mod
        run: |
          GOV=$(awk '/^go / {print $2}' go.mod)
          echo "GO_VERSION=$GOV" >> $GITHUB_ENV

      - name: Build
        run: python ./release.py
        env:
          CGO_ENABLED: '0'
          VERSION: ${{ env.VERSION }}

      - name: Delete existing release and tag
        uses: actions/github-script@v8
        env:
          TAG_NAME: ${{ env.VERSION }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const tag = process.env.TAG_NAME;
            const { owner, repo } = context.repo;

            try {
              const release = await github.rest.repos.getReleaseByTag({
                owner,
                repo,
                tag,
              });
              await github.rest.repos.deleteRelease({
                owner,
                repo,
                release_id: release.data.id,
              });
              core.info(`deleted existing release for tag ${tag}`);
            } catch (error) {
              if (error.status !== 404) {
                throw error;
              }
              core.info(`no existing release for tag ${tag}`);
            }

            try {
              await github.rest.git.deleteRef({
                owner,
                repo,
                ref: `tags/${tag}`,
              });
              core.info(`deleted existing tag ${tag}`);
            } catch (error) {
              if (error.status !== 404 && error.status !== 422) {
                throw error;
              }
              core.info(`no existing tag ${tag}`);
            }

      - name: Publish
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION }}
          target_commitish: ${{ inputs.ref }}
          name: "Release ${{ env.VERSION }} | ${{ env.RELEASE_NAME_SUFFIX }}"
          files: './release/mosdns*.zip'
          prerelease: false
          generate_release_notes: true
          body: |
            ${{ env.RELEASE_SUBTITLE }}
            Built with Go ${{ env.GO_VERSION }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
