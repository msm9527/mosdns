# 文件用途:
# - 定义 `requery` 插件实例，负责“记忆规则刷新任务”的队列与调度。
# - `domain_output` 的脏数据通知会把任务提交到这里。
# 参数说明:
# - `file`: requery 调度配置持久化文件（由 Web UI 修改）。
plugins:
  - tag: requery
    type: requery
    args:
      file: "webinfo/requeryconfig.json"
