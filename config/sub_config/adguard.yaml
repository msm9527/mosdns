# 文件用途:
# - 维护广告规则数据源插件 `adguard_rule`。
# - `rule_set.yaml` 中的 `unified_matcher1` 会引用该 tag 参与分流标记（fast_mark 5）。
# 参数说明:
# - `dir`: 规则文件目录。
# - `socks5`: 拉取/更新远程规则时使用的代理。
plugins:
  - tag: adguard
    type: adguard_rule
    args:
      dir: "adguard/"
      socks5: "127.0.0.1:7891"
