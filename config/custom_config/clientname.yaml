# 用户自定义客户端别名配置
#
# 这个文件是客户端 IP -> 显示别名 的长期配置真源。
# - 前端“客户端别名”保存会直接改这个文件，并立即生效。
# - 你也可以手工修改这个文件，然后重启 mosdns 生效。
# - 这里不再走 control.db；数据库只保留运行态和生成态数据。
#
# 格式说明：
# - key: 客户端 IP，支持 IPv4/IPv6
# - value: 展示名称，例如 手机 / 客厅电视 / NAS

"127.0.0.1": "msm本机"
